package version

var (
	// Version is a build variable representing the release version.
	Version = "Unknown"

	// CommitHash is a build variable representing the commit hash.
	CommitHash = "Unknown"
)
